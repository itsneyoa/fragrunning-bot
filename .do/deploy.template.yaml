spec:
 name: fragrunning-bot
 services:
 - name: fragrunning-bot
   git:
     branch: master
     repo_clone_url: https://github.com/itsneyoa/fragrunning-bot.git
  build_command: yarn
  run_command: node index.js